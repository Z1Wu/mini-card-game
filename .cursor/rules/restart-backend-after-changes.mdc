---
description: 修改完功能后重启后端
alwaysApply: true
---

# 后端/前端修改后重启后端

- 每次修改**后端**逻辑或接口后，**主动重启后端**，确保改动生效。
- 每次修改**前端**逻辑后，也**主动重启后端**，便于联调与验证。
- 重启方式：在项目根目录下执行  
  `cd backend && uv run python main.py`  
  并在后台运行（或由用户在本机终端执行）。
- 若本次对话中已改过 `backend/` 或 `frontend/` 下代码，在收尾时提醒用户「代码已改，需要重启后端再测」，并询问是否需要代为执行重启命令。

UID=1000
GID=1000
# Local development defaults. Production uses APP_ENV=production and requires a mounted
# AUTH_USERS_FILE plus concrete, comma-separated public origins (never use *).
APP_ENV=development
ALLOWED_ORIGINS=http://localhost:3000
MAX_MESSAGES_PER_SECOND=30
ALLOW_LEGACY_JOIN_GAME=false
